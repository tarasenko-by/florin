# тест — managed by Veritheme

Revision `28ecb4507f07`, Veritheme `1.1.1`.

Everything in this directory is generated. Veritheme opens a pull request
with a new copy on every release, so edits made here are replaced the next
time one lands. Change the system instead, then release it:

- https://stage.veritheme.com/projects/?id=f96fbe16-669c-43dc-b603-1bba18940648

## Using these files

- `theme.css` — load it **after** `veritheme.min.css`.
- `tokens.json` — import into the Veritheme Figma plugin, then Apply.
- `veritheme.system.json` — the manifest: revision, library version, hashes.

Directory: `src/styles/veritheme`
